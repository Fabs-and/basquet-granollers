---
const {fixedLinks} = Astro.props
---

<ul class='top-footer-articles'>
  {
    fixedLinks.map((link) => (
      <li>
        <a
          href={`${link.link}`}
        >
          <h5>{link.name.toUpperCase()}</h5>
        </a>
      </li>
    ))
  }
</ul>

<style>
  ul {
    width: 13.75rem;
  }
</style>