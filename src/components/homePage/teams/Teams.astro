---
import TeamItem from "@components/homePage/teams/TeamItem.astro";
import HomePageSectionLayout from "@layouts/HomePageSectionLayout.astro";
import { fetchImagesInPageBySlug } from "fetch-wordpress-api";

const { needsHeading } = Astro.props;
const teams = await fetchImagesInPageBySlug("equips1");
---

<HomePageSectionLayout sectionTitle="Els equips del Club Bàsquet Granollers">
  <div class="teams-container">
    {
      teams.reverse().map((team) => (
        <div>
          <TeamItem {team} />
        </div>
      ))
    }
  </div>
</HomePageSectionLayout>
<!-- <section class="g-page-section-with-slider-container">
  <div class="g-section-container">
    {needsHeading && <h2>Els equips del Club Bàsquet Granollers</h2>}
  </div>
</section> -->

<style>
  .teams-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    padding-right: var(--padding-inline);
  }

  @media (width < 1385px) {
    .teams-container {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      place-items: center;
      gap: 1rem;
    }

    .teams-container > :nth-child(1) {
      justify-self: end;
    }

    .teams-container > :nth-child(2) {
      justify-self: start;
    }

    .teams-container > :nth-child(3) {
      justify-self: end;
    }

    .teams-container > :nth-child(4) {
      justify-self: start;
    }
  }

  @media (width < 1025px) {
   .teams-container {
    padding-right: var(--padding-inline-tablet);
   }
  }
  @media (width < 754px) {
    .teams-container {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
  }
  @media (width < 648px) {
     .teams-container {
    padding-right: var(--padding-inline-mobile);
   }
  }

</style>
