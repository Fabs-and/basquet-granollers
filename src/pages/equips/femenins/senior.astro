---
import PlayersLayout from "@layouts/PlayersLayout.astro";

import { seniorTeamsData } from "@data/apiData";

const { femaleSenior } = seniorTeamsData;
import { getSeasonYears } from "@utils/helperFunctions";
const { firstYear, secondYear } = getSeasonYears();

const heading = femaleSenior!.title;
const title = `Sènior primer femení temporada 20${firstYear}-${secondYear}`;
---

<PlayersLayout {heading} {title} team={femaleSenior!.players} />
