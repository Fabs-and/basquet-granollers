---
//Import layouts
import PlayersLayout from "@layouts/PlayersLayout.astro";

//Import data
import { seniorTeamsData } from "@data/apiData";
const { maleSenior} = seniorTeamsData;
import { getSeasonYears } from "@utils/helperFunctions";
const { firstYear, secondYear } = getSeasonYears();

const heading = maleSenior!.title;
const title = `Sènior primer masculí temporada 20${firstYear}-${secondYear}`;
---

<PlayersLayout
  {heading}
  {title}
  team={maleSenior!.players}
/>
