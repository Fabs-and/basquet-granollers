---
import TeamsLayout from "@layouts/TeamsLayout.astro";

import { coachesImage, coachesTitle } from "@data/apiData";
import { getSeasonYears } from "@utils/helperFunctions";
const { firstYear, secondYear } = getSeasonYears();

const heading = coachesTitle[0].title.rendered;
const title = `Cost tècnic temporada 20${firstYear}-${secondYear}`;
---

<TeamsLayout teamsImages={coachesImage} {title} {heading} />
