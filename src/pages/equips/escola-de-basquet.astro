---
import TeamsLayout from "@layouts/TeamsLayout.astro";
import { schoolTeamImage, schoolTeamTitle } from "@data/apiData";


import { getSeasonYears } from "@utils/helperFunctions";
const { firstYear, secondYear } = getSeasonYears();

const heading = schoolTeamTitle[0].title.rendered;
const title = `Escola de bàsquet temporada 20${firstYear}-${secondYear}`;
---

<TeamsLayout teamsImages={schoolTeamImage} {heading} {title}/>