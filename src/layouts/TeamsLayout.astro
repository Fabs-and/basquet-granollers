---
import InternalPageLayout from "./NonDynamicPageLayout.astro";
const { teamsImages, heading, subHeading, title } = Astro.props;
---

<InternalPageLayout {title} {heading} {subHeading}>
  <div class="images-container">
    {
      teamsImages.map((image, i) => (
        <div class="image-container">
          <img src={image.url} alt={image.alt && image.alt} loading={i < 3 ? "eager" : "lazy"}  />
        </div>
      ))
    }
  </div>
</InternalPageLayout>

<style>
  .images-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
  }

  .image-container {
    max-width: 595px;
    /* height: 18.4rem; */
    border-top-left-radius: 2.9375rem;
    overflow: hidden;
  }

  img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @media (width < 848px) {
    .image-container {
      width: 100%;
      height: auto;
    }
  }

  @media (width < 415px) {
    .image-container {
      width: 100vw;
      margin-inline: calc(var(--padding-inline-mobile) * -1);
    }
  }
</style>
